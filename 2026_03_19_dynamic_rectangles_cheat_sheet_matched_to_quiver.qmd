---
subtitle: ""
description: ""
# bibliography: manual-refs.bib
format:
  html:
    resources:
      - reference-backlinks.js
    include-after-body:    
      - text: |
          # <script type="text/javascript" src="reference-backlinks.js"></script>
    default: true         
    code-copy: true
    code-link: true        # This adds individual buttons
    code-fold: true        # Hide code by default, show on click
    code-summary: "Show the code"
    code-overflow: wrap
    code-block-bg: "#FAEBD7"
    code-block-border-left: "#31BAE9"
    embed-resources: false     # Set to 'false' for drafts. Override if you're NOT sharing output
    include-in-header:
      - text: 
          <link href="https://fonts.googleapis.com/css2?family=Lato&display=swap" rel="stylesheet">
          <link href="https://fonts.googleapis.com/css2?family=Fira+Mono&display=swap" rel="stylesheet">
          <link href="https://fonts.googleapis.com/css2?family=Montserrat&display=swap" rel="stylesheet">
          <link href="https://fonts.googleapis.com/css2?family=Cabin&display=swap" rel="stylesheet">
          <link href="https://fonts.googleapis.com/css2?family=Roboto+Slab&display=swap" rel="stylesheet">
          <link href="https://fonts.googleapis.com/css2?family=Schoolbell&display=swap" rel="stylesheet">
      - header.html
    css:
      - swart.css
      - tachyons.min.css
      - r-colors.css
    fontsize: 18pt
    lightbox: true
    page-layout: full
    fig-width: 8.5
    fig-height: 11
    fig-dpi: 300
    html-math-method: katex
    df-print: paged
    toc: true
    toc-float: true
    citeproc: true
    link-citations: true
    linestretch: 1.0
    
    
    
  typst:
    fig-width: 8.5
    fig-height: 11
    fig-dpi: 300
    margin:
      x: 0.25in
      y: 0.25in
    toc: false
    fontsize: 16pt
    mainfont: "Cabin"
  
    
  revealjs:
    slide-number: true
    transition: fade
    code-overflow: wrap
    center: true
    smaller: true
    scrollable: true
    chalkboard: true
    multiplex: false
    theme: solarized
    reference-location: margin
    logo: img/red-cross-640-435.png
    footer: "Footer text"
    code-block-height: 650px
  # docx:
  #   highlight-style: github
  #   fig_caption: true
editor: source
quarto:
  render:
    cache-refresh: true
# for .qmd filesd
execute:
  echo: false
  message: false
  warning: false
  eval: true
  fig-width: 13
  fig-height: 8
# for .rmd files
knitr:
  opts_chunk:
    echo: false
    error: false
    warning: false
    message: false
    cache: false
---

```{r}
#| label: setup

library(ggplot2)

##### Key tuning parameters if you need adjustments:
#####   
#####   title_space — room above each rectangle for the title
#####   caption_space — room below each rectangle for the caption
#####   row_gap — breathing room between rows
#####   margin — internal margin (your YAML handles page margins)
#####   title_offset — distance from title text to top of rectangle
#####   caption_offset — distance from caption text to bottom of rectangle
#####   header_height — space for main title and date
#####   rect_h <- 1.25   # inches - increase/decrease until rectangles are desired size

sqrt_phi <- sqrt((1 + sqrt(5)) / 2)  # sqrt of phi = 1.2720196495
phi      <- (1 + sqrt(5)) / 2        # phi = 1.6180339887

# Editable title and date
chart_title <- "Height x Multiplier = Length of Base"
chart_date <- paste("Revised:", format(Sys.Date(), "%B %d, %Y"))

# Ratios ordered by base length (ratio value)
# Matches the full set used in the companion "quiver of arrows" file
ratios <- list(
  list(name = "Root 1 (a square)",              ratio = 1,          label = "Multiplier = 1 = √1"),
  list(name = "Root-Phi Rectangle",  ratio = sqrt_phi,   label = sprintf("Multiplier = %.4f = √φ", sqrt_phi)),
  list(name = "Root 2 Rectangle",    ratio = sqrt(2),    label = sprintf("Multiplier = %.4f = √2", sqrt(2))),
  list(name = "Root 2.25 (1.5 Rectangle)", ratio = sqrt(2.25), label = sprintf("Multiplier = %.4f = √2.25", sqrt(2.25))),
  list(name = "Phi Rectangle",       ratio = phi,        label = sprintf("Multiplier = %.4f = φ",  phi)),
  list(name = "Root 3 Rectangle",    ratio = sqrt(3),    label = sprintf("Multiplier = %.4f = √3", sqrt(3))),
  list(name = "Root 4 Rectangle",    ratio = sqrt(4),    label = sprintf("Multiplier = %.4f = √4", sqrt(4))),
  list(name = "Root 5 Rectangle",    ratio = sqrt(5),    label = sprintf("Multiplier = %.4f = √5", sqrt(5))),
  list(name = "Root 6 Rectangle",    ratio = sqrt(6),    label = sprintf("Multiplier = %.4f = √6", sqrt(6))),
  list(name = "Root 7 Rectangle",    ratio = sqrt(7),    label = sprintf("Multiplier = %.4f = √7", sqrt(7))),
  list(name = "Root 9 Rectangle",    ratio = sqrt(9),    label = sprintf("Multiplier = %.4f = √9", sqrt(9)))
)

create_frame_sheet <- function(page_width, page_height) {
  margin <- 0.0
  work_width <- page_width - 2 * margin
  work_height <- page_height - 2 * margin
  
  # Reserve space for header
  header_height <- 1.00   # ADJUST THIS AS NEEDED
  content_top <- work_height - header_height
  
  # Two columns
  col_gap <- 0.3
  col_width <- (work_width - col_gap) / 2
  
  # Number of rectangles per column (auto-balances the full set)
  n_per_col <- ceiling(length(ratios) / 2)
  
  # FIXED HEIGHT FOR ALL RECTANGLES - ADJUST THIS VALUE
  rect_h <- 1.10  # inches - increase/decrease until rectangles are desired size
  
  # Fixed spacing for title and caption blocks
  title_space <- 0.22   # reserved space above rectangle for title block
  caption_space <- 0.22 # reserved space below rectangle for caption block
  
  # Direct control for the gap between one caption and the next title
  # Increase this value to add more space below the caption and above the next title
  between_text_gap <- 0.55
  
  # Distance from text baseline to rectangle edge
  # Increase title_offset to move titles higher above rectangles
  # Increase caption_offset to move captions farther below rectangles
  title_offset <- 0.12
  caption_offset <- 0.12
  
  # Convert the desired caption-to-next-title gap into the row gap used by the layout
  row_gap <- between_text_gap - title_space - caption_space + caption_offset + title_offset
  if (row_gap < 0) row_gap <- 0
  
  total_row_height <- title_space + rect_h + caption_space
  total_step_height <- total_row_height + row_gap
  available_step_height <- content_top / n_per_col
  
  # Auto-fit safeguard: if the requested row height + gap will not fit on the page,
  # shrink the rectangle height so the final row stays visible.
  if (total_step_height > available_step_height) {
    rect_h <- max(0.55, available_step_height - title_space - caption_space - row_gap)
    total_row_height <- title_space + rect_h + caption_space
    total_step_height <- total_row_height + row_gap
  }
  
  # Left margin within each column for "h" label
  left_pad <- 0.25
  
  rects <- data.frame(
    xmin = numeric(), xmax = numeric(), 
    ymin = numeric(), ymax = numeric(),
    title = character(), caption = character(),
    title_x = numeric(), title_y = numeric(),
    caption_x = numeric(), caption_y = numeric(),
    h_x = numeric(), h_y = numeric(),
    stringsAsFactors = FALSE
  )
  
  for (i in seq_along(ratios)) {
    r <- ratios[[i]]
    
    # Determine column placement
    col <- ifelse(i <= n_per_col, 1, 2)
    row_in_col <- ifelse(i <= n_per_col, i, i - n_per_col)
    
    # Column left edge
    col_left <- ifelse(col == 1, 0, col_width + col_gap)
    
    # Calculate vertical position from top down
    # Row 1 starts at content_top, each subsequent row is below
    row_top <- content_top - (row_in_col - 1) * total_step_height
    
    # Position elements within row
    rect_top <- row_top - title_space
    rect_bottom <- rect_top - rect_h
    
    # Position text with offset from rectangle edges
    extra_title_drop <- if (grepl("Root 2.25 Rectangle", r$name, fixed = TRUE)) 0.06 else 0
    title_y <- rect_top + title_offset - extra_title_drop
    caption_y <- rect_bottom - caption_offset
    
    # Width based on aspect ratio (width = height * ratio)
    rect_w <- rect_h * r$ratio
    
    # Left-align within column
    rect_left <- col_left + left_pad
    
    rects <- rbind(rects, data.frame(
      xmin = rect_left,
      xmax = rect_left + rect_w,
      ymin = rect_bottom,
      ymax = rect_top,
      title = r$name,
      caption = r$label,
      title_x = rect_left,
      title_y = title_y,
      caption_x = rect_left,
      caption_y = caption_y,
      h_x = rect_left - 0.10,
      h_y = (rect_top + rect_bottom) / 2
    ))
  }
  
  # Header positions
  header_y <- work_height - header_height / 2
  
  p <- ggplot2::ggplot() +
    # Header: title on left, date above
    ggplot2::annotate("text", x = 0, y = 9.5, label = "** Root 2 Example: If height = 36cm, base = 36cm x 1.4142 = 50.9cm",
                      size = 5, fontface = "bold", hjust = 0, vjust = 0.25) +
    ggplot2::annotate("text", x = 0, y = 10, label = chart_title,
                      size = 10, fontface = "bold", hjust = 0, vjust = 0.5) +
    ggplot2::annotate("text", x = work_width, y = 10.5, label = chart_date,
                      size = 4.5, hjust = 1, vjust = 0.5) +
    # Rectangles
    ggplot2::geom_rect(
      data = rects,
      ggplot2::aes(xmin = xmin, xmax = xmax, ymin = ymin, ymax = ymax),
      fill = NA, color = "black", linewidth = 2
    ) +
    ggplot2::geom_text(
      data = rects,
      ggplot2::aes(x = title_x, y = title_y, label = title),
      size = 5, fontface = "bold", hjust = 0, vjust = 0, lineheight = 0.9
    ) +
    ggplot2::geom_text(
      data = rects,
      ggplot2::aes(x = caption_x, y = caption_y, label = caption),
      size = 4.5, hjust = 0, vjust = 1
    ) +
    ggplot2::geom_text(
      data = rects,
      ggplot2::aes(x = h_x, y = h_y, label = "1"),
      size = 4.5, fontface = "italic", hjust = 1
    ) +
    ggplot2::coord_fixed(ratio = 1, xlim = c(0, work_width), ylim = c(0, work_height), clip = "off") +
    ggplot2::theme_void() +
    ggplot2::theme(
      plot.margin = ggplot2::margin(5, 5, 5, 5),
      aspect.ratio = NULL
    )
  
  p
}
```

```{r}
#| label: portrait
#| fig-width: 7.5
#| fig-height: 10
#| fig-align: left

create_frame_sheet(7.5, 10)
```
